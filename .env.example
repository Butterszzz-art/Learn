# Required for deep-dive generation (the level-matched, web-search-grounded
# explainers), applied insights, and News for any interest without a
# curated RSS/API source (custom interests, Business, Political Science).
# Curated News still works without it.
ANTHROPIC_API_KEY=

# Optional. Model used for summarization/categorization/generation.
ANTHROPIC_MODEL=claude-sonnet-5

# Optional. Powers the Research Agent (/research) — an add-on feature, fully
# independent of the Anthropic key above. It's an AI agent (via OpenRouter)
# that searches OpenAlex + Semantic Scholar for real papers before answering
# a research question, citing what it actually found. Leave unset and every
# other feature in the app (Deep Dives, News, Library, etc.) is unaffected —
# the Research tab just shows a "not configured" message.
OPENROUTER_API_KEY=

# Optional. Model used by the Research Agent, as an OpenRouter model slug
# (e.g. "anthropic/claude-sonnet-5", "openai/gpt-4o", "google/gemini-2.5-pro").
# Defaults to anthropic/claude-sonnet-5.
OPENROUTER_MODEL=

# Optional. OpenAlex asks for a contact email in the "polite pool" for
# better rate limits (not a key — just an email string). Leave unset to use
# OpenAlex's default anonymous rate limit.
OPENALEX_MAILTO=

# Optional. Raises Semantic Scholar's rate limit above the public tier.
SEMANTIC_SCHOLAR_API_KEY=

# --- Hosted deployment only (leave both unset for local dev — the app uses
# a local SQLite file at ./data/neuro-digest.db instead) ---
# A Turso (libSQL) database URL and auth token. Create one at turso.tech,
# or via the CLI: `turso db create digest && turso db show digest --url`
# and `turso db tokens create digest`. See the README's deployment section.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# --- Hosted deployment only (leave unset for local dev) ---
# A shared passphrase that gates the whole app behind a login page, so a
# public URL can't be used by strangers to spend your Anthropic API budget.
# Pick your own value; there's no separate signup.
SITE_PASSWORD=
