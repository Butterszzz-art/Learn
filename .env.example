# Optional. Without this, the app falls back to keyword-based categorization,
# raw-snippet summaries, and a static seed bank of brain facts (no live generation).
ANTHROPIC_API_KEY=

# Optional. Model used for summarization/categorization/brain-fact generation.
ANTHROPIC_MODEL=claude-sonnet-5
